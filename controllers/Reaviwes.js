const { PrismaClient } = require('@prisma/client')
const prisma = new PrismaClient();


const Rivewis =async (req,res)=>{
    const { productID, userid, Rating, reaction }=req.body;
    try {

        
    } catch (error) {
        
    }
}